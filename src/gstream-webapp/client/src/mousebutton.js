/**
 * gStream Mouse Button Definitions
 * Bit positions used in the mouse state button bitmask.
 * These correspond to the bitmask in MouseState where each bit
 * represents one mouse button (bit 0 = Left, bit 1 = Right, etc.).
 */
export const MouseButtons = {
  Left: 0,
  Right: 1,
  Middle: 2,
  Forward: 3,
  Back: 4
};
